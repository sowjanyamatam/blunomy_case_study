[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "lidar-catenary"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []   # or paste your requirements.txt entries here

[tool.setuptools.packages.find]
where = ["src"]